import createAuthRouter, { AuthMiddlewares, AuthRouterConfigurator } from "./authRouter";

// For ES6 imports
export default createAuthRouter;
export { AuthMiddlewares, AuthRouterConfigurator };

module.exports = createAuthRouter;
